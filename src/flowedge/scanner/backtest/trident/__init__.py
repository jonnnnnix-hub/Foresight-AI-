"""Trident — ETF index 0DTE options scalping engine.

Three-pronged (SPY, QQQ, IWM), both directions (calls + puts),
0–90 minute hold windows, same-day only. Signal-optimised via
combinatorial backtest grid search over 2+ years of 1-min data.
"""
